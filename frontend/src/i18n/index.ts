import en from './locales/en.json';

/**
 * i18n placeholder. Wire up react-i18next (or similar) here when enabling the
 * `featureFlags.enableI18n` flag. Locale JSON lives in ./locales.
 */
export const resources = { en } as const;
export const defaultLocale = 'en';
